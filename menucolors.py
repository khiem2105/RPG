"""module to define constant in class..."""
import pygame


class Menu_colors:
    """constant in class..."""

    def __init__(self):
        self.black = (0, 0, 0)
        self.color_inactive = pygame.Color("SaddleBrown")
        self.color_active = pygame.Color("Brown")
